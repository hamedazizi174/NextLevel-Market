export const USER = "user";
export const CREATED_AT = "createdAt";
export const NAME = "name";

export enum SORT {
  ASC = "asc",
  DESC = "desc",
}

export enum ROLES {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum TOKENS {
  ACCESS = "accessToken",
  REFRESH = "refreshToken",
}
